import styled from 'styled-components'
import { color } from '../../../shared/styles/variables'

export const AcceptChangesWrap = styled.button`
    width: 100%;
    height: 39px;
    border-radius: 8px;
    background-color: ${color.borderBlue};
    font-size: 14px;
    line-height: 19px;
    color: ${color.defaultText};
`
