import styled from 'styled-components'
import { color } from '../../../../shared/styles/variables'

export const RemoveWrap = styled.button`
    margin-top: 10px;
    margin-bottom: 10px;
    font-family: 'Axiforma', sans-serif;
    font-size: 14px;
    line-height: 22px;
    color: ${color.removeBlue};
`
